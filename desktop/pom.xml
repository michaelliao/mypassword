<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.puppylab</groupId>
	<artifactId>mypassword</artifactId>
	<version>1.0</version>
	<packaging>jar</packaging>

	<properties>
		<mainClass>org.puppylab.mypassword.ui.MainWindow</mainClass>

		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<project.version>1.0</project.version>
		<maven.compiler.source>25</maven.compiler.source>
		<maven.compiler.target>25</maven.compiler.target>
		<maven.compiler.release>25</maven.compiler.release>
		<java.version>25</java.version>

		<!-- artifact version -->
		<jackson.version>2.21.1</jackson.version>
		<junit.version>5.14.3</junit.version>
		<persistence.version>3.2.0</persistence.version>
		<logback.version>1.5.32</logback.version>
		<nimbus.jwt.version>10.9</nimbus.jwt.version>
		<slf4j.version>2.0.17</slf4j.version>
		<sqlite.version>3.51.2.0</sqlite.version>
		<swt.version>3.133.0</swt.version>

		<!-- plugin version -->
		<maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
		<exec-maven-plugin.version>3.6.3</exec-maven-plugin.version>
		<maven-dependency-plugin.version>3.10.0</maven-dependency-plugin.version>
		<maven-jar-plugin.version>3.5.0</maven-jar-plugin.version>
		<app.name>MyPassword</app.name>
		<jpackage.input>${project.build.directory}/jpackage-input</jpackage.input>
		<jlink.runtime>${project.build.directory}/jlink-runtime</jlink.runtime>
		<jlink.modules>
			java.base,java.desktop,java.net.http,java.sql,jdk.httpserver</jlink.modules>
		<!-- Overridden per-OS profile; macOS needs -XstartOnFirstThread for
		SWT/Cocoa. -->
		<jpackage.javaOptions>-Dfile.encoding=UTF-8</jpackage.javaOptions>
	</properties>

	<profiles>
		<profile>
			<id>win64</id>
			<activation>
				<os>
					<family>windows</family>
					<arch>amd64</arch>
				</os>
			</activation>
			<properties>
				<os.type>windows-amd64</os.type>
				<jpackage.type>app-image</jpackage.type>
				<path.sep>;</path.sep>
				<swt.artifactId>org.eclipse.swt.win32.win32.x86_64</swt.artifactId>
				<icon.file>logo.ico</icon.file>
			</properties>
		</profile>
		<profile>
			<id>macos-arm64</id>
			<activation>
				<os>
					<family>mac</family>
					<arch>aarch64</arch>
				</os>
			</activation>
			<properties>
				<os.type>mac-aarch64</os.type>
				<jpackage.type>app-image</jpackage.type>
				<path.sep>:</path.sep>
				<swt.artifactId>org.eclipse.swt.cocoa.macosx.aarch64</swt.artifactId>
				<icon.file>logo.icns</icon.file>
				<jpackage.javaOptions>-XstartOnFirstThread</jpackage.javaOptions>
			</properties>
		</profile>
		<profile>
			<id>macos-x64</id>
			<activation>
				<os>
					<family>mac</family>
					<arch>x86_64</arch>
				</os>
			</activation>
			<properties>
				<os.type>mac-x86_64</os.type>
				<jpackage.type>app-image</jpackage.type>
				<path.sep>:</path.sep>
				<swt.artifactId>org.eclipse.swt.cocoa.macosx.x86_64</swt.artifactId>
				<icon.file>logo.icns</icon.file>
				<jpackage.javaOptions>-XstartOnFirstThread</jpackage.javaOptions>
			</properties>
		</profile>
		<profile>
			<id>linux64</id>
			<activation>
				<os>
					<family>unix</family>
					<name>linux</name>
					<arch>amd64</arch>
				</os>
			</activation>
			<properties>
				<os.type>linux-amd64</os.type>
				<jpackage.type>app-image</jpackage.type>
				<path.sep>:</path.sep>
				<swt.artifactId>org.eclipse.swt.gtk.linux.x86_64</swt.artifactId>
				<icon.file>logo.png</icon.file>
			</properties>
		</profile>
	</profiles>

	<dependencies>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>${jackson.version}</version>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${slf4j.version}</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>${logback.version}</version>
		</dependency>
		<dependency>
			<groupId>org.xerial</groupId>
			<artifactId>sqlite-jdbc</artifactId>
			<version>${sqlite.version}</version>
		</dependency>
		<dependency>
			<groupId>jakarta.persistence</groupId>
			<artifactId>jakarta.persistence-api</artifactId>
			<version>${persistence.version}</version>
		</dependency>
		<dependency>
			<groupId>com.nimbusds</groupId>
			<artifactId>nimbus-jose-jwt</artifactId>
			<version>${nimbus.jwt.version}</version>
		</dependency>
		<dependency>
			<groupId>org.eclipse.platform</groupId>
			<artifactId>${swt.artifactId}</artifactId>
			<version>${swt.version}</version>
		</dependency>

		<!-- test dependency -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<finalName>mypassword</finalName>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven-compiler-plugin.version}</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>${maven-dependency-plugin.version}</version>
				<executions>
					<execution>
						<id>copy-dependencies</id>
						<phase>package</phase>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
						<configuration>
							<outputDirectory>${jpackage.input}/lib</outputDirectory>
							<includeScope>runtime</includeScope>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>${maven-jar-plugin.version}</version>
				<configuration>
					<archive>
						<manifest>
							<mainClass>${mainClass}</mainClass>
							<addClasspath>true</addClasspath>
							<classpathPrefix>lib/</classpathPrefix>
						</manifest>
					</archive>
					<outputDirectory>${jpackage.input}</outputDirectory>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>${exec-maven-plugin.version}</version>
				<executions>
					<execution>
						<id>jlink</id>
						<phase>package</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<executable>jlink</executable>
							<arguments>
								<argument>--add-modules</argument>
								<argument>${jlink.modules}</argument>
								<argument>--output</argument>
								<argument>${jlink.runtime}</argument>
								<argument>--no-header-files</argument>
								<argument>--no-man-pages</argument>
								<argument>--strip-debug</argument>
								<argument>--compress</argument>
								<argument>zip-9</argument>
							</arguments>
						</configuration>
					</execution>
					<execution>
						<id>jpackage</id>
						<phase>package</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<executable>jpackage</executable>
							<arguments>
								<argument>--type</argument>
								<argument>${jpackage.type}</argument>
								<argument>--name</argument>
								<argument>${app.name}</argument>
								<argument>--icon</argument>
								<argument>${project.basedir}/src/main/resources/${icon.file}</argument>
								<argument>--input</argument>
								<argument>${jpackage.input}</argument>
								<argument>--main-jar</argument>
								<argument>${project.build.finalName}.jar</argument>
								<argument>--main-class</argument>
								<argument>${mainClass}</argument>
								<argument>--runtime-image</argument>
								<argument>${jlink.runtime}</argument>
								<argument>--dest</argument>
								<argument>${project.build.directory}/dist</argument>
								<argument>--java-options</argument>
								<argument>${jpackage.javaOptions}</argument>
							</arguments>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
